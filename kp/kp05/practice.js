function checkMatching(){const ok=cpuMatch.value==='process'&&ramMatch.value==='temporary'&&psuMatch.value==='power';kp05State.matching=ok;matchingResult.innerHTML=ok?'<b>✅ Semua betul.</b><p>CPU memproses, RAM menyimpan sementara dan PSU membekalkan kuasa.</p>':'<b>❌ Semak semula fungsi komponen.</b>';if(ok)updateProgress()}
function checkSelection(){const ok=slowPc.value==='ram'&&slowStorage.value==='ssd'&&noDisplay.value==='gpuRam';kp05State.selection=ok;selectionResult.innerHTML=ok?'<b>✅ Semua cadangan tepat.</b>':'<b>❌ Semak semula simptom dan komponen.</b>';if(ok)updateProgress()}
function checkClassification(){const ok=keyboardClass.value==='input'&&monitorClass.value==='output'&&ssdClass.value==='storage';kp05State.classification=ok;classificationResult.innerHTML=ok?'<b>✅ Semua kategori betul.</b>':'<b>❌ Semak semula kategori.</b>';if(ok)updateProgress()}